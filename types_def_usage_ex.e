types_def_usage_ex.e
  
  
  Types to be used in the example of Type MIN/MAX value (type_min_max_usage_ex.e)
  
  The types have to defined in a file compiled/loaded before the file that 
  uses the macros.

  
<'
type addr_t : int (bits : 8);

type color_t : [WHITE, BLACK, RED];

'>
 